# OpenCreator Personal Burmese Dubbing — Kaggle Cells
#
# GitHub workflow:
# 1) ဒီ project folder ကို GitHub repository ထဲ push လုပ်ပါ။
# 2) Kaggle Notebook > Settings > Accelerator မှာ GPU (T4/P100) ရွေးပါ။
# 3) Internet ကို On လုပ်ပါ။
# 4) အောက်က cells တွေကို အစဉ်လိုက် run ပါ။
# 5) နောက်ဆုံး cell ထွက်လာတဲ့ Cloudflare https://*.trycloudflare.com public URL ကိုဖွင့်ပါ။
# 6) Gemini API key ကို UI > Open Settings ထဲမှာထည့်ပါ။ Transcript တစ်ခုလုံးကို batch အနည်းငယ်နဲ့ ဘာသာပြန်ပြီး local F5 Myanmar TTS နဲ့အသံထုတ်ပါမယ်။

# =============================
# CELL 1 — GitHub ကနေ code clone
# =============================
from pathlib import Path
import os

REPO_URL = "https://github.com/surviveman78-commits/opencreator-kaggle-dubbing.git"
BRANCH = "main"
APP_DIR = Path('/kaggle/working/opencreator-kaggle-dubbing')

# ဒီ repo URL က user ရဲ့ public GitHub repository ဖြစ်ပါတယ်။
if not APP_DIR.exists():
    !git clone --depth 1 --branch $BRANCH $REPO_URL $APP_DIR
else:
    print('Project folder already exists:', APP_DIR)

assert (APP_DIR / 'custom_ui.py').exists(), 'GitHub repo ထဲမှာ custom_ui.py မတွေ့ပါ။'
assert 'start_public_tunnel' in (APP_DIR / 'custom_ui.py').read_text(), 'Public-link v2 code မတွေ့ပါ။ GitHub folder အဟောင်းကိုဖျက်ပြီး clone ပြန်လုပ်ပါ။'
print('Project:', APP_DIR)

# =============================
# CELL 2 — Kaggle GPU/CUDA စစ်ဆေး
# =============================
!nvidia-smi

# ဒီ cell က GPU မရှိရင် error ဖြစ်နိုင်ပါတယ်။
# Kaggle Notebook Settings မှာ Accelerator = GPU ရွေးထားကြောင်း စစ်ပါ။

# =============================
# CELL 3 — FFmpeg နဲ့ font packages
# =============================
!apt-get update -qq
!apt-get install -y -qq ffmpeg fonts-noto-core fonts-noto-extra
!ffmpeg -version | head -1

# =============================
# CELL 4 — Python models/packages install
# =============================
%cd $APP_DIR
!pip install -q -r requirements.txt

# faster-whisper transcription က CUDA ကိုတွေ့ရင် GPU သုံးမယ်။ Translation က Gemini batch API သုံးပါမယ်။

# =============================
# CELL 5 — UI launch
# =============================
# Custom UI ပွင့်လာပြီးမှ Gemini API key ကို UI ထဲမှာထည့်ပါ။
%cd $APP_DIR
# custom_ui.py downloads cloudflared and prints a https://*.trycloudflare.com public link
!python custom_ui.py

# =============================
# UI အသုံးပြုပုံ
# =============================
# 1) Video upload သို့မဟုတ် public video URL ထည့်ပါ။
# 2) Preview Box နှိပ်ပါ။
# 3) Video ပေါ်က Liquid Glass blur box ကို လက်နဲ့ drag လုပ်ပါ။
# 4) အောက်ညာဘက် glass handle နဲ့ ချုံ့/ချဲ့ပါ။
# 5) ⚙ Open Settings ထဲမှာ Gemini/Groq key, custom font, subtitle color ရွေးပါ။
# 6) Start Burmese Dubbing နှိပ်ပါ။ Translation က fixed Gemini batch ဖြစ်ပြီး TTS က fixed local F5 Myanmar TTS ဖြစ်ပါတယ်။
# 7) Output video ကို preview ကြည့်ပြီး Download dubbed MP4 ကိုနှိပ်ပါ။
# 8) Browser refresh လုပ်လည်း opencreator_runtime/jobs.json ရှိနေသရွေ့ နောက်ဆုံးအခြေအနေကိုပြန်တင်ပါမယ်။
#
# Final output video မှာ preview UI ရဲ့ glass border/handle/label မပါပါ။
# User သတ်မှတ်ထားတဲ့ fixed blur rectangle နေရာမှာပဲ FFmpeg blur ထည့်ပါမယ်။
